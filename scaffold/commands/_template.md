---
description: ""
supported_by: []
requires: []
auto_run_after: []
globs: []
---

# Command: /{nome}

## Input

<!-- Descreva os argumentos e flags aceitos.
     Ex: `$SELECTION` ou `$FILE` (obrigatório)
         `--flag` → descrição do que faz (default: valor) -->

## Steps

<!-- Passos que a IA deve seguir ao executar este command.
     Referencie hooks, skills e memory quando relevante.
     Use {{commands.typecheck}}, {{commands.lint}}, etc. para comandos do projeto. -->

1. ...
2. Verificar `memory/mistakes.md` para armadilhas conhecidas no domínio
3. Rodar `{{commands.typecheck}}` antes de finalizar

## Output

<!-- Descreva o formato esperado da saída.
     Se for estruturado, coloque um exemplo. -->

## Referências de projeto

- Typecheck: `{{commands.typecheck}}`
- Lint: `{{commands.lint}}`
- Test: `{{commands.test}}`

## Notas

<!-- Regras de comportamento específicas deste command.
     O que nunca fazer, edge cases, comportamento esperado em falha. -->
